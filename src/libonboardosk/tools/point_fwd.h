#ifndef POINTDECLS_H
#define POINTDECLS_H

// no includes here, move those to point.h

template<class T> class TVec2;
typedef TVec2<double> Vec2;

template<class T> class TPoint;
typedef TPoint<double> Point;

template<class T> class TSize;
typedef TSize<double> Size;

template<class T> class TOffset;
typedef TOffset<double> Offset;

template<class T> class TScale;
typedef TScale<double> Scale;

template<class T> class TLine;
typedef TLine<double> Line;

#endif // POINTDECLS_H


